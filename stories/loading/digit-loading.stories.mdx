import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { DigitLoading } from "../../src";
import DigitProvidersDecorator from "../../.storybook/DigitProvidersDecorator";

<Meta
    title="Elements|DigitLoading"
    component={DigitLoading}
    decorators={[DigitProvidersDecorator]}
/>

# DigitLoading

A circular spinning animation that is nice to symbolize loading. The color is your primary color.

## Size variation

<Preview>
    <Story name="Size 20 loading">
        <DigitLoading size={20}/>
    </Story>
    <Story name="Size 40 loading">
        <DigitLoading size={40}/>
    </Story>
    <Story name="Size 60 loading">
        <DigitLoading size={60}/>
    </Story>
    <Story name="Size 80 loading">
        <DigitLoading size={80}/>
    </Story>
    <Story name="Size 100 loading">
        <DigitLoading size={100}/>
    </Story>
</Preview>

<Props of={DigitLoading} />
