import { Meta, Story, Canvas, Props } from "@storybook/addon-docs/blocks";
import { DigitMarkdown } from "../../src";
import DigitProvidersDecorator from "../../.storybook/DigitProvidersDecorator";

<Meta title="Others/DigitMarkdown" component={DigitMarkdown} />

# DigitMarkdown

Renders markdown with react-digit-components tags, e.g. heading comes from `DigitText` and the table is from `DigitTable`.

## Markdown

<Canvas>
    <Story name="Size 20 loading">
        <DigitMarkdown markdownSource="Hello, **this** is *markdown*." />
    </Story>
</Canvas>

<Props of={DigitMarkdown} />
