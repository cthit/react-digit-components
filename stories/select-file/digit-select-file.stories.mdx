import { Fragment, useState } from "react";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import {
    DigitButton,
    DigitSelectFile
} from "../../src";
import * as yup from "yup";

# DigitSelectFile

Select a file a la rdc style.

<Meta title="Others/DigitSelectFile" component={DigitSelectFile} />

## Flat DigitSelectFile

<Preview>
    <Story name="Flat file select">
        {() => {
            const [file, setFile] = useState(null);
            return (
                <React.Fragment>
                    <DigitSelectFile
                        selectedFileName={file != null ? file.name : null}
                        onSelectFile={setFile}
                        buttonProps={{raised: true}}
                    />
                    <DigitButton disabled={file == null} primary raised onClick={() => console.log(file)} text="Upload" />
                </React.Fragment>
            );
        }}
    </Story>
</Preview>