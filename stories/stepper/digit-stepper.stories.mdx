import { useState } from "react";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { DigitStepper, DigitButton } from "../../src";
import { Center } from "../../src/styles/digit-layout/DigitLayout.styles";
import DigitProvidersDecorator from "../../.storybook/DigitProvidersDecorator";

<Meta
    title="Navigation/DigitStepper"
    component={DigitStepper}
/>

# DigitStepper

A useful way to tell a user where they are during a multistep process. Be aware of using to many steps because of mobile users.  

<Preview>
    <Story name="Normal">
        {() => {
            const [activeStep, setStep] = useState(0);
            return (
                <div>
                    <DigitStepper
                        activeStep={activeStep}
                        steps={[
                            {
                                text: "First step of this stepper",
                            },
                            {
                                text: "The second of the cool cooler stepper",
                            },
                            {
                                text: "Whaaaaaaat",
                            },
                            {
                                text: "Almost finished",
                            },
                            {
                                text: "Finished",
                            },
                        ]}
                    />
                    <Center>
                        <DigitButton
                            primary
                            raised
                            text="Next"
                            onClick={() => {
                                setStep((activeStep + 1) % 6);
                            }}
                        />
                    </Center>
                </div>
            );
        }}
    </Story>
</Preview>

## Other starting step

<Preview>
    <Story name="Other start">
        {() => {
            const [activeStep, setStep] = useState(2);
            return (
                <div>
                    <DigitStepper
                        activeStep={activeStep}
                        steps={[
                            {
                                text: "First step of this stepper",
                            },
                            {
                                text: "The second of the stepper",
                            },
                            {
                                text: "WhaaaaOnly a few steps to go",
                            },
                            {
                                text: "So close",
                            },
                            {
                                text: "You did it!",
                            },
                        ]}
                    />
                    <Center>
                        <DigitButton
                            primary
                            raised
                            text="Next"
                            onClick={() => {
                                setStep((activeStep + 1) % 6);
                            }}
                        />
                    </Center>
                </div>
            );
        }}
    </Story>
</Preview>

<Props of={DigitStepper} />
