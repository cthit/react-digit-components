import { Fragment, useState } from "react";
import { Meta, Story, Canvas, Props } from "@storybook/addon-docs/blocks";
import { DigitTabs, DigitProviders } from "../../src";
import { DigitTabs as DigitTabsProps } from "../../src/components/digit-tabs/DigitTabs.element.jsx";
import DigitProvidersDecorator from "../../.storybook/DigitProvidersDecorator";

import Home from "@material-ui/icons/Add";
import Date from "@material-ui/icons/DateRange";
import Info from "@material-ui/icons/Info";

<Meta title="Navigation/DigitTabs" component={DigitTabsProps} />

# DigitTabs

A mobile, tablet and desktop friendly way of navigating between multiple panes. Should always be positioned as high on the website as possible. If you want a mobile version of `DigitTabs`, then there's `DigitBottomNavigation`. `

## DigitTabs with fullWidth

<Canvas>
    <Story name="DigitTabs with fullWidth">
        {() => {
            const [selected, setSelected] = useState("feed");
            return (
                <DigitTabs
                    onChange={value => setSelected(value)}
                    selected={selected}
                    tabs={[
                        { text: "Home", value: "home" },
                        { text: "Feed", value: "feed" },
                        { text: "Settings", value: "settings" }
                    ]}
                    fullWidth
                />
            );
        }}
    </Story>
</Canvas>

## DigitTabs centered

<Canvas>
    <Story name="DigitTabs centered">
        {() => {
            const [selected, setSelected] = useState("feed");
            return (
                <DigitTabs
                    onChange={value => setSelected(value)}
                    selected={selected}
                    tabs={[
                        { text: "Home", value: "home" },
                        { text: "Feed", value: "feed" },
                        { text: "Settings", value: "settings" }
                    ]}
                    centered
                />
            );
        }}
    </Story>
</Canvas>

## DigitTabs scrollable with only icons

<Canvas>
    <Story name="DigitTabs scrolalble with only icons">
        {() => {
            const [selected, setSelected] = useState("feed");
            return (
                <DigitTabs
                    onChange={value => setSelected(value)}
                    selected={selected}
                    tabs={[
                        { value: "home", icon: Home },
                        { value: "feed", icon: Date },
                        { value: "settings", icon: Info }
                    ]}
                />
            );
        }}
    </Story>
</Canvas>

## DigitTabs centered with icons and text

<Canvas>
    <Story name="DigitTabs centered with icons">
        {() => {
            const [selected, setSelected] = useState("feed");
            return (
                <DigitTabs
                    onChange={value => setSelected(value)}
                    selected={selected}
                    tabs={[
                        { text: "Home", value: "home", icon: Home },
                        { text: "Feed", value: "feed", icon: Date },
                        { text: "Settings", value: "settings", icon: Info }
                    ]}
                    centered
                />
            );
        }}
    </Story>
</Canvas>

## DigitTabs that scrolls

<Canvas>
    <Story name="DigitTabs that scrolls">
        {() => {
            const [selected, setSelected] = useState("settings");
            return (
                <DigitTabs
                    onChange={value => setSelected(value)}
                    selected={selected}
                    tabs={[
                        { text: "Home", value: "home" },
                        { text: "Feed", value: "feed" },
                        { text: "Settings", value: "settings" },
                        { text: "Account", value: "account" },
                        { text: "Something", value: "something" },
                        { text: "Something else", value: "something_else" }
                    ]}
                />
            );
        }}
    </Story>
</Canvas>

## DigitTabs width titleFont

<Canvas>
    <Story name="DigitTabs with fullWidth and titleFont">
        {() => {
            const [selected, setSelected] = useState("feed");
            return (
                <DigitTabs
                    onChange={value => setSelected(value)}
                    selected={selected}
                    tabs={[
                        { text: "Home", value: "home" },
                        { text: "Feed", value: "feed" },
                        { text: "Settings", value: "settings" }
                    ]}
                    fullWidth
                    titleFont
                />
            );
        }}
    </Story>
</Canvas>

<Props of={DigitTabsProps} />
