import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { DigitTooltip, DigitButton } from "../../src";
import DigitProvidersDecorator from "../../.storybook/DigitProvidersDecorator";

<Meta
    title="Others/DigitTooltip"
    component={DigitTooltip}
/>

# DigitTooltip

Used to give more context to an action or some type of information.

## Tool tip

<Preview>
    <Story name="Tooltip">
        <DigitTooltip text="This is a tooltip" render={() => (
            <DigitButton text="Hover me" />
        )}/>
    </Story>
    <Story name="Tooltip text">
        <DigitTooltip text="You can change the text" render={() => (
            <DigitButton text="Hover me too" />
        )}/>
    </Story>
</Preview>

<Props of={DigitTooltip} />
